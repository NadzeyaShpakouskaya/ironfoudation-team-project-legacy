import Foundation

extension AppStyle {
    /// Provides the sizes for UI elements using within the application.
    enum UIElementSize {
        static let stroke = 12.0
        static let cornerRadius = 10.0
        static let minPadding = 4.0
        static let avatarSideSize = 80.0
    }
}
