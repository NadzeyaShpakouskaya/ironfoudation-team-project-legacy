import Foundation

extension Owner {
    /// Provides the necessary values for work of `Current Owner`.
    enum StorageInformation {
        /// The name of storage associated with the owner.
        static let storageName = "OwnerStorage"
        /// The key associated with the owner.
        static let ownerKey = "owner"
    }
}
